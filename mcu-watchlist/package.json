{
  "name": "mcu-watchlist",
  "version": "1.0.0",
  "scripts": {
    "install:all": "npm install && cd server && npm install && cd ../client && npm install",
    "dev": "concurrently -n client,server -c cyan,magenta \"npm run dev --prefix client\" \"npm run dev --prefix server\"",
    "start": "concurrently -n client,server -c cyan,magenta \"npm run preview --prefix client\" \"npm start --prefix server\""
  },
  "dependencies": {
    "concurrently": "^8.2.0"
  }
}
